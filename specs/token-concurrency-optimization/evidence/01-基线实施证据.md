# 阶段 01：基线实施证据

## 状态

`blocked`

门禁实现和离线验证已完成，但没有运行真实外部模型 API 的完整流程，因此不能形成 Token 基线，也不能进入阶段 02。

## 已实施

- 增加最小请求数门禁，0 请求不再判定为通过。
- 增加完整流程开始、Guidance 完成、CoachReport 完成、流程完成和流程失败计数。
- Guidance 必须收到 `done` 且 `complete=true`。
- CoachReport 必须收到无错误的 `done` 和非空报告。
- 增加非敏感基线标签校验和脱敏 Token 聚合工具。
- 聚合结果仅包含任务、Provider、模型、用量来源、状态、调用数、Token、重试和耗时；不输出账号或会话标识。
- 跟踪的 Locust 指引不再保存明文密码。

## 验证结果

- 阶段 01 单元测试：20 项通过。
- Python 语法检查：通过。
- Locust `--list` 加载检查：通过。
- 0 用户、0 请求场景：退出码 1。
- 缺少测试密码场景：退出码 2，且退出门禁保留原非零状态。
- PostgreSQL 聚合 SQL：使用无数据未来时间窗只读执行成功。
- 跟踪指引直接密码赋值扫描：0 项。

## 尚缺基线

以下指标尚未生成，不能填 0：

- 完整流程模型调用总数。
- 各任务输入、输出 Token。
- `provider`、`estimated`、`unavailable` 用量分布。
- Guidance、15 轮预演和 CoachReport 的真实耗时与质量快照。

## 阻塞条件

1. 疑似泄露的测试凭据需要在外部完成轮换，并仅通过环境变量注入。
2. 需要明确授权一次 1 用户、15 轮完整流程的外部模型 API 用量和费用。
3. 完成真实基线后才能将阶段 01 标记为 `completed`。

## 回滚

只需回退阶段 01 的 Locust 门禁、聚合工具、单元测试和本证据文档；不涉及业务数据、数据库结构、业务 API 或容器。
